/** Map `games.slug` → App Router segment under `/practice/[mode]`. */
export const GAME_SLUG_TO_PATH: Record<string, string> = {
  full_scale: 'full-scale',
  full_chord: 'full-chord',
  sequence: 'sequence',
  progression_251: '251',
  interval: 'interval',
};
